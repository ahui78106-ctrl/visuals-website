{
  "name": "visuals-upgraded-site",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "start": "npx live-server dist --port=5173 --open=./index.html"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "fs-extra": "^11.2.0"
  }
}
